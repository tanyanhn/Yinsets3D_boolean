#include<fstream>
#include<iostream>
#include<vector>
#include "../src/Yinset.h"

using namespace std;
using namespace YSB;

void exportdata(string s, YinSet<Real> y)
{
    ofstream outfile(s);
    if(outfile.is_open())
    {
        
    }
}